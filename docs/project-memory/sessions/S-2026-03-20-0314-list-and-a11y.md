---
id: S-2026-03-20-0314-list-and-a11y
title: Sprint 18 — List rendering, keyboard accessibility, and visual polish
goal: Fix unordered list CSS, tab order in sidebar, hr visibility, aria-labels, and heading differentiation
context: Sprint 18 agentA branch; all changes scoped to public/index.html
plan:
  - B-046: Add list-style:disc to ul CSS (JS renderer already creates ul/li)
  - B-047: Fix hr border color to rgba(255,255,255,0.15)
  - B-045: Increase heading size/weight differentiation (H1/H2/H3)
  - B-049: Add aria-label to New Chat button and Copy buttons
  - B-048+B-043: Add tabindex="-1" to delete-btn, tabindex="0"+role="button"+keydown to history entries
---

## Changes Made

- `public/index.html` CSS:
  - `hr`: changed border-top color from `var(--border)` to `rgba(255,255,255,0.15)`, margin to `12px 0` (B-047)
  - `.bubble h1/h2/h3`: explicit rem sizes (1.5/1.25/1.05), distinct weights, H2 muted color (#cbd5e0), H3 uppercase+letter-spacing (#a0aec0) (B-045)
  - `.bubble ul`: added `padding: 0; list-style: disc;`, updated margin to `6px 0 6px 20px` (B-046)
- `public/index.html` HTML:
  - `#new-chat-btn`: added `aria-label="Start a new conversation"` (B-049)
- `public/index.html` JS (`renderHistoryList`):
  - history entry div: added `tabindex="0"` and `role="button"` (B-043)
  - delete button: added `tabindex="-1"` (B-048)
  - history entry: added keydown listener for Enter/Space to load conversation (B-043)
  - copy button: added `aria-label="Copy message"` (B-049)

## Decisions Made

- B-046: The unordered list JS renderer already creates `<ul><li>` elements (added in a prior sprint). The bug was CSS-only — `list-style: disc` was missing from `.bubble ul`.
- B-045: Switched from `em` to `rem` units for heading sizes to avoid compounding with bubble's own font-size.
- B-043/B-048: Delete buttons get `tabindex="-1"` (removed from tab order but still mouse-clickable). History entries become focusable rows with Enter/Space activating load.

## Commits

- (link after commit)
