---
title: "S-2026-03-20-0003-js-error-ux"
goal: "Implement B-010, B-012, B-013 — friendly error UX in sendMessage(), 404 stale history UX, and stale model guard"
context: "Sprint 9 agentB task. Agent owns sendMessage() error handler, loadConversation() 404 branch, and model init."
---

## Plan

1. B-010: Add `friendlyErrorMessage()` helper; replace raw error display in `sendMessage()` with mapped human-readable text + optional `<details>` toggle for raw error.
2. B-012: In `loadConversation()` 404 branch, keep entry in localStorage/sidebar; show error bubble with "Remove from history" button that cleans up on click.
3. B-013: After `modelSelector.value = savedModel`, validate that the value stuck; clear stale localStorage key if not.

## Changes Made

- `public/index.html`:
  - Added `friendlyErrorMessage(raw)` helper before `sendMessage()`
  - Updated `parsed.error` handler to use friendly messages with `<details>` fallback
  - Updated `catch` block in `sendMessage()` to use friendly messages
  - Updated `loadConversation()` 404 branch: removed immediate localStorage removal; added "Remove from history" button in error bubble
  - Added stale model guard after `modelSelector.value = savedModel`

## Decisions Made

- Network errors detected by searching for "fetch", "network", "Failed to fetch", "NetworkError" in the raw message — covers both browser fetch failures and server-returned strings.
- `<details>`/`<summary>` used for the raw error fallback — native HTML, no JS needed, matches brief spec.
- `firstToken` guard preserved in error paths so loading dots are removed before showing error text.
