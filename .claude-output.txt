---

**Files changed**
- `public/index.html` — all 5 tasks

**Commands run**
- `node -e "require('fs')..."` — HTML readable check ✓
- `npm audit --audit-level=high` — 0 vulnerabilities ✓
- `git commit` + `git push -u origin HEAD`

**What was implemented**

| Task | Change |
|------|--------|
| B-046 | Regex normalization of `•` and lone `*` → `-` at top of parse loop (lines added before blockquote check) |
| B-050 | Added `lastLi`/`currentNestedList` state; `/^  - /` lines create nested `<ul>` inside the last top-level `<li>`; `currentNestedList` resets on each new top-level bullet |
| B-047 | HR `rgba(255,255,255,0.15)` → `0.3` |
| B-051 | `role="dialog"`, `aria-modal="true"`, `aria-labelledby="confirm-modal-title"` on modal; `id` on `<p>`; `aria-label` on both buttons |
| B-052 | `aria-label="Select AI model"` on `<select>` |

**Notes**
- Nested list support is one level only (2-space indent), per brief constraint.
- `* ` bullet normalization uses a negative lookahead `\*(?!\*)` so `**bold**` inline syntax is unaffected.
